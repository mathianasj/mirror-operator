# NOTE: For development/testing only. Production users should install
# mirror-operator from the community-operators-prod catalog.
apiVersion: operators.coreos.com/v1alpha1
kind: CatalogSource
metadata:
  name: mirror-operator-catalog
  namespace: openshift-marketplace
spec:
  sourceType: grpc
  image: quay.io/mathianasj/mirror-operator-catalog:v0.0.1
  displayName: Mirror Operator (Dev)
  publisher: mathianasj
  updateStrategy:
    registryPoll:
      interval: 10m
